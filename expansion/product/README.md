# Product data root for Keep Expansion.
#
# Production releases place signed/encrypted agent definitions, presets, and
# assets under this tree. Developer/foundation builds may leave it empty —
# default agents are still seeded into USER state via seed_defaults /
# provision (never write user memories/journals here).
#
# See expansion.state_layout and docs/KEEP_EXPANSION_MIGRATION.md.
