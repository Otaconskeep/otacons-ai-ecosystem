# Foundation architecture

The core detects normalized hardware without exposing CUDA IDs, assigns logical GPU roles by capability, builds data-driven agents, and writes runtime configuration outside the repository. Service installation is intentionally mocked in this milestone.
