let state={step:0,scan:null,name:'',features:['chat','memory','voice'],config:null}; const page=document.getElementById('page');
const labels=['Welcome','System Scan','Hardware Recommendation','Agent Setup','Features','Review','Finish'];
function render(){let s=state.step; document.getElementById('title').textContent=labels[s]; if(s===0) page.innerHTML='<p>This wizard configures your local AI system. No Docker or YAML knowledge is required.</p><button onclick="next()">Get Started</button>'; if(s===1) page.innerHTML='<p>Detect your computer, GPUs, storage, and available capacity.</p><button onclick="scan()">Scan My System</button>'; if(s===2){let h=state.scan.hardware.hardware,g=state.scan.hardware.gpu_roles; page.innerHTML=`<div class=card>System: ${h.os}<br>CPU: ${h.cpu.model} (${h.cpu.cores} cores)<br>Memory: ${h.ram_gb} GB</div>`+(h.gpus.length?h.gpus.map(x=>`<div class=card>${x.model} — ${x.vram_gb} GB VRAM — ${x.capability}</div>`).join(''):'<div class=card>CPU fallback (no GPU detected)</div>')+`<p class=muted>Recommended primary: ${g.primary_gpu}</p><button onclick="next()">Use Recommended Setup</button>`;} if(s===3) page.innerHTML='<label>Agent name<br><input id=name placeholder="Billy"></label><label>Role<br><input value="Primary Assistant" disabled></label><button onclick="setName()">Continue</button>'; if(s===4) page.innerHTML=['chat','memory','voice','image_generation','video_generation','web_search'].map(f=>`<label><input type=checkbox value=${f} ${state.features.includes(f)?'checked':''}> ${f.replaceAll('_',' ')}</label>`).join('')+'<button onclick="setFeatures()">Continue</button>'; if(s===5) page.innerHTML=`<div class=card>Agent: ${state.name}<br>Features: ${state.features.join(', ')}<br>Storage: ${(state.scan.storage.find(x=>x.recommended)||state.scan.storage[0]||{}).path||'Unavailable'}</div><button onclick="build()">Create Configuration</button>`; if(s===6) page.innerHTML=`<p>${state.name} is configured.</p><p class=muted>Services are not installed in this milestone.</p><div class=card>${state.config?.saved||''}</div>`;}
function next(){state.step++;render()} async function scan(){state.scan=await (await fetch('/api/scan')).json();state.step=2;render()} function setName(){state.name=document.getElementById('name').value.trim()||'Assistant';next()} function setFeatures(){state.features=[...document.querySelectorAll('input[type=checkbox]:checked')].map(x=>x.value);next()} async function build(){let p=await (await fetch('/api/plan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:state.name,features:state.features})})).json();let s=await (await fetch('/api/save',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({config:p.config})})).json();state.config={saved:s.path};next()} render();
